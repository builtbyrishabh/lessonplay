{
  "name": "lessonplay-game-engine",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Self-contained Learn Loop engine + reference template. Copied into the sandbox and installed there; not part of the web app's dependency graph.",
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "packages/*",
    "games/chemistry-lab-bench"
  ],
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
